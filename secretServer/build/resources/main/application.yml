server:
  port: 8080
spring:
  application:
    name: api-secret-server
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://74.207.224.64:5002/test_database?useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
    username: ingeniero
    password: raspberry

bd:
  sp:
    createUser: CALL  secret_server.SP_CREATE_USER(:id,:name,:email,:password,:image,:role,:google);
